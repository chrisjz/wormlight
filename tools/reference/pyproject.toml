[project]
name = "wormlight-reference"
version = "0"
description = "Reference solutions for Wormlight's port and production checks"
requires-python = ">=3.12"
dependencies = ["numpy>=2.1", "scipy>=1.14"]

[tool.uv]
package = false
